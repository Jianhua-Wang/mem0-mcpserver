{
  "name": "mem0-mcpserver",
  "version": "1.0.0",
  "description": "Serverless MCP memory server — Cloudflare Workers + D1 + Vectorize",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "agents": "^0.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250312.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
